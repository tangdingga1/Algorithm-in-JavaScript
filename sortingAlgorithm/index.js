const { bubbleSort, bubbleSortFunctionWay } = require('./bubbleSort');
const { selectionSort } = require('./selectionSort');
const { quickSortBase, quickSortInPlace } = require('./quickSort');

module.exports = {
    bubbleSort,
    bubbleSortFunctionWay,
    selectionSort,
    quickSortBase,
    quickSortInPlace,
}

